// Convex function stubs.
// The dashboard currently uses Next.js API routes plus local mission state so the
// playable slice works even without a configured Convex deployment. This file is
// intentionally kept lightweight until `npx convex dev` is wired and generated
// server bindings are available.
export {};
